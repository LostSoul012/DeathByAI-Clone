PORT=3001
CLIENT_ORIGIN=http://localhost:5173

# Required for real AI judging. Get one free at https://console.groq.com
GROQ_API_KEY=

# Only for testing — points the judge calls at a local mock server instead
# of the real Groq API. Leave unset in normal/production use.
# GROQ_API_BASE_URL=http://localhost:4570
